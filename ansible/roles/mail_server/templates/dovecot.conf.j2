# Dovecot Configuration
# Generated by Hibana Stack

protocols = imap pop3 lmtp
listen = *, ::

# SSL/TLS (using self-signed certificates initially)
ssl = required
ssl_cert = </etc/ssl/certs/hibana-{{ primary_domain }}.crt
ssl_key = </etc/ssl/private/hibana-{{ primary_domain }}.key
ssl_min_protocol = TLSv1.2

# Mail location
mail_location = maildir:/var/mail/vhosts/%d/%n
mail_privileged_group = vmail

# Authentication
auth_mechanisms = plain login

passdb {
  driver = passwd-file
  args = scheme=SHA512-CRYPT username_format=%u /etc/dovecot/users
}

userdb {
  driver = static
  args = uid=vmail gid=vmail home=/var/mail/vhosts/%d/%n
}

# IMAP configuration
protocol imap {
  mail_plugins = $mail_plugins
}

# POP3 configuration
protocol pop3 {
  mail_plugins = $mail_plugins
}

# LMTP configuration
protocol lmtp {
  postmaster_address = postmaster@{{ primary_domain }}
}

# Postfix SASL auth
service auth {
  unix_listener /var/spool/postfix/private/auth {
    mode = 0660
    user = postfix
    group = postfix
  }
}

# Master user configuration
namespace inbox {
  inbox = yes
}
